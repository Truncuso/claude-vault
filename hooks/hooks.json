{
  "SessionStart": [
    {
      "command": "qmd-daemon ensure || true",
      "timeout": 10000,
      "description": "Ensure QMD HTTP daemon is running on port ${QMD_DAEMON_PORT}. Non-blocking on failure."
    },
    {
      "command": "vault info || true",
      "timeout": 5000,
      "description": "Print active vault summary for session context. Non-blocking on failure."
    }
  ],
  "FileChanged": [
    {
      "pattern": "**/*.md",
      "command": "qmd update",
      "mode": "async",
      "debounce_ms": 30000,
      "description": "Re-index QMD on markdown file changes (global qmd update)"
    }
  ],
  "PostCompact": [
    {
      "command": "qmd-daemon status || true",
      "timeout": 5000,
      "description": "Verify QMD daemon health after context compaction. Non-blocking on failure."
    }
  ],
  "Stop": [
    {
      "command": "qmd status --json 2>/dev/null | python3 -c \"import sys,json; d=json.load(sys.stdin); print(f'QMD: {d.get(\\\"document_count\\\",0)} docs indexed')\" || true",
      "description": "Report QMD index freshness on session end"
    }
  ]
}
